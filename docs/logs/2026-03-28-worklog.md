# 2026-03-28 Worklog

Date: 2026-03-28
Repository: ai-prompt-kc-2026
Team: Team Codex KC

Note: path references in this historical log reflect the repo layout on 2026-03-28. Superseded discovery, product, and plan docs now live under `docs/archive/legacy/2026-03-28-competition-package/`.

## Timeline

### 10:15 AM CT

- Working question: what kind of solution can feel real in 60 days instead of speculative?
- Current belief: the answer should use existing infrastructure and existing partner networks instead of inventing a new logistics system.
- Evidence or rationale: the challenge rewards problem understanding, polish, and adaptability. A lightweight pilot beats a grand platform.
- Next move: find a single city-native idea that can support product, business, and brand artifacts.

### 10:30 AM CT

- Working question: can StreetCart KC support all three tracks without becoming too cute?
- Current belief: yes, if the streetcar is treated as the public-facing spine and partner agencies do the neighborhood fulfillment.
- Evidence or rationale: the concept is visually strong, Kansas City-specific, and easy for judges to remember. It also avoids relying on direct streetcar cargo operations in phase one.
- Next move: anchor the company around one thesis and pressure-test feasibility.

### 10:40 AM CT

- Working question: which part of the challenge should drive the work first?
- Current belief: Oracle should lead. The business plan, 60-day pilot, and KPI logic create the constraints for Muse and Architect.
- Evidence or rationale: if the pilot math and pilot geography are weak, a demo or brand system will look decorative instead of serious.
- Next move: use the challenge API to identify the highest-signal endpoints and lock the pilot geography.

### 10:47 AM CT

- Working question: which challenge API endpoints are worth the team's time?
- Current belief: `harvest`, `pantries`, `supply-alerts`, and `demographics` are the core inputs; `311-calls` and `food-atlas` are supporting evidence; `store-closures` is context; `transit` is low priority.
- Evidence or rationale: sampled responses gave strong ZIP priority, partner, language, and urgency signals. The transit data looked patchy and bus-oriented, which is not ideal for the current concept spine.
- Next move: write a one-page data strategy and map each endpoint to Oracle, Muse, and Architect.

### 10:51 AM CT

- Working question: should the free bus system replace the streetcar as the main concept spine?
- Current belief: the bus is the technically correct answer. The streetcar is the right answer for this brief, because the judges are not just scoring logistics, they are scoring whether a city councilmember would want to stand in front of it.
- Evidence or rationale: buses likely provide broader coverage, but the streetcar is more ownable, more visual, more Kansas City-specific, and far more cohesive across Oracle, Muse, and Architect.
- Decision: keep the streetcar as the spine.
- Guardrail: acknowledge buses without making them the hero.
- Next move: add one line to Oracle materials that says Phase 2 expansion uses KC's free bus network to extend reach beyond the streetcar corridor.

### 10:55 AM CT

- Working question: how do we show reasoning as we work instead of only presenting finished artifacts?
- Current belief: maintain a dated worklog folder and record decisions as they happen.
- Evidence or rationale: the challenge explicitly rewards AI mastery and evidence of iteration. A live reasoning log is proof, not just polish.
- Next move: keep appending to this file as major concept, budget, pilot, and messaging decisions get locked.

### 11:20 AM CT

- Working question: which ZIPs, hubs, and partners should become the shared Oracle + Muse source of truth?
- Current belief: lock five pilot ZIPs (`64130`, `64127`, `66101`, `66105`, `64132`), two visible streetcar-facing hubs (River Market and Union Station), and two partner feeder hubs (Guadalupe Family Hub and Cross-Lines Connector Hub).
- Evidence or rationale: that mix keeps the streetcar as the public spine while honestly extending reach into bilingual and no-vehicle neighborhoods without pretending the corridor solves the whole problem by itself.
- Decision: use those facts as the controlling input for Oracle and Muse.
- Guardrail: keep buses and feeder operations as supporting reach, not the hero story.

### 11:30 AM CT

- Working question: how should the Oracle deck change without rebuilding the whole story?
- Current belief: keep the PowerPoint as the primary artifact, but rebuild it from a cleaner source so it follows the exact judge-first structure: thesis, proof, operating loop, pilot geography, budget, KPIs, Muse proof, live proof, AI mastery, curveball, close.
- Evidence or rationale: the earlier deck direction had usable ideas, but the package needed one coherent data source and tighter control over the economics, hub logic, and narrative order.
- Decision: generate a new `11`-slide Oracle deck from source instead of editing slide-by-slide by hand.

### 11:40 AM CT

- Working question: what should Muse emphasize so judges read it as civic design instead of marketing fluff?
- Current belief: Muse should borrow the KC Streetcar visual language, but keep households served, priority ZIPs, and partner fulfillment visible in every asset. The bracket and wrap stay as energy and reward layers, not the lead story.
- Evidence or rationale: the strongest Muse artifacts make the concept feel unmistakably local while still proving that this is about food access first.
- Decision: structure Muse around a brand brief, a transit-inspired scoreboard mock, a corridor-plus-ZIP visual, a renewal-phase wrap concept, and bilingual resident copy.

### 11:50 AM CT

- Working question: what is the minimum Architect proof that increases confidence without stealing time from Oracle?
- Current belief: one thin site is enough if it shows the operator dashboard, resident instructions, and public leaderboard in under two minutes.
- Evidence or rationale: those three views map directly to the judges' questions. Anything broader starts to look like unfinished software instead of a disciplined pilot proof.
- Decision: ship a static live demo backed by one shared JSON fact base and link it directly to the deck and worklog.

### 11:55 AM CT

- Working question: did the artifacts actually build and line up?
- Current belief: yes. The Oracle deck now exports cleanly as `docs/plans/StreetCart_KC_Oracle.pptx`, the site loads locally without console errors, and the resident language toggle and leaderboard flow both work.
- Evidence or rationale: deck export passed, slide text was verified across all `11` slides, Quick Look generated a deck thumbnail, and browser QA passed on `http://127.0.0.1:4173/`.
- Decision: treat the package as the new baseline and use the next block for copy cleanup, curveball adaptation, and any final visual tightening.

### 12:05 PM CT

- Working question: should we keep side artifacts that did not make the final concept?
- Current belief: yes, if they are clearly labeled as process evidence instead of primary deliverables.
- Evidence or rationale: preserving intermediate work helps show iteration quality and AI-assisted exploration without confusing judges about the final direction.
- Decision: archive the Gemini-generated food access dashboard under `docs/research` and link it lightly from the site as an exploration artifact.

### 12:20 PM CT

- Working question: should the team narrow to Oracle + Muse or keep all three challenges live?
- Current belief: keep all three, but stop pretending the effort should be balanced. Oracle is the controlling artifact, Muse is the polish layer, and Architect stays a thin proof.
- Evidence or rationale: the repo already has a usable three-view demo, so dropping Architect entirely would leave track coverage on the table. Expanding it into a real app would weaken the stronger deliverables.
- Decision: lock the priority order as Oracle first, Muse second, Architect fixed at operator / resident / leaderboard scope.
- Guardrail: no Next.js scaffold, backend setup, auth, SMS integration, or other product expansion unless Oracle and Muse are fully locked.
- Next move: align the README, shared JSON, thin demo copy, deck script, and verification notes to this scope decision.

### 12:30 PM CT

- Working question: how should the repo capture the KC Streetcar live signage feed without scraping brittle screen HTML?
- Current belief: use the public Firebase backend behind the signage site, not DOM scraping. The HTML is only a Vue shell, while the real public data sits in `stops`, `alerts`, `displays`, and `byStop/*`.
- Evidence or rationale: `https://arrivals.kcstreetcar.org/rmn` loads `kiosk.js`, which binds directly to the anonymous Firebase Realtime Database at `https://kiosk-6e6b4.firebaseio.com`. A direct fetch gives structured arrivals, stop names, timestamps, and occupancy data.
- Decision: add a small Node scraper plus one normalized JSON snapshot under `docs/reference/kc-streetcar/arrivals/`.
- Guardrail: keep this as a reference ingestion utility only. Do not expand it into app scaffolding, polling infrastructure, or a production service.

### 12:45 PM CT

- Working question: what is the smallest real product shape for a branded KC Streetcar tracker with food access context?
- Current belief: build a stop-centric React tracker, not a map-first explorer. Keep arrivals live from the public signage backend and precompute nearby food access within 1 mile of each stop group.
- Evidence or rationale: the official KC Streetcar homepage exposes stable brand cues, the official KCATA GTFS feed exposes streetcar stop geometry for route `601`, and public OSM data is good enough for groceries and markets when combined with curated pantry partners already named in the repo docs.
- Decision: replace the static landing page with a real tracker app backed by one generated route/food dataset plus live arrival polling.
- Guardrail: do not add a backend service or heavy GIS stack. The app should stay client-side with small build scripts.

### 12:45 PM CT

- Working question: how should the team turn the public KC Streetcar website into a usable Muse-facing brand guideline?
- Current belief: extract the brand as a civic design system, not as a marketing style board. The site is strongest when read as a disciplined public-utility identity built around blue, route clarity, service language, and Kansas City place context.
- Evidence or rationale: the live site, public theme CSS, the 2014 KC Streetcar brand reveal post, and the wrap/media kit all point to the same pattern: simple transit language, HalisR headlines, Avenir body copy, a cobalt-to-deep-blue core palette, and strong "not a billboard" restraint.
- Decision: create `docs/product/brand.md` as a reverse-engineered, repo-usable brand guide for Muse work.
- Guardrail: treat the document as an inferred guide, not an official legal brand standards manual.

### 2:15 PM CT

- Working question: what should be pushed right now versus kept local?
- Current belief: publish the repo's actual working set, but keep oversized raw reference media local unless there is a specific need for Git LFS or external asset storage.
- Evidence or rationale: the current branch already exists on `origin`, but the local workspace includes a large batch of uncommitted docs, scripts, data, site, and app files plus roughly `365 MB` of reference videos under `docs/reference/kc-streetcar/videos/`. Pushing those binaries directly would add cost and repo weight without improving the core submission package.
- Decision: update `.gitignore` to keep the raw video dump local, append this checkpoint to the worklog, then stage, commit, and push the publishable project artifacts on the existing branch.
- Guardrail: do not delete or rewrite any local work; only separate local-only reference assets from the tracked repo.

### 2:20 PM CT

- Working question: what is the smallest credible way to add a real KC Streetcar tracker without blowing up the existing submission site?
- Current belief: keep the landing page intact and add the tracker as its own React/Vite page. Treat the route stop order from the official signage endpoint list as canonical, and use GTFS only for geometry matching and food-radius calculations.
- Evidence or rationale: the first GTFS pass dropped River Market West and scrambled stop order because one representative trip was not stable across route variants. Using all route trips to pick the most common stop match per direction fixes the geometry bug, while a separate `tracker.html` keeps the new app reversible.
- Decision: ship `tracker.html` plus `src/tracker/`, bundle `data/kc-streetcar-tracker.json` directly into the app, poll the public Firebase signage feed client-side, and keep the original static page as a linked artifact.
- Guardrail: no backend, no map dependency, no heavy transit SDK. Keep the tracker stop-first, branded, and readable on one page.

### 2:32 PM CT

- Working question: how should the package absorb the curveball without becoming a disconnected side memo?
- Current belief: make the curveball a full-package sync. The fact base, Oracle deck, thin demo, and worklog should all tell the same two-part response: resident support in `66101` / `66105` and a `1,000-pound` produce surge inside a `48-hour` window.
- Evidence or rationale: the repo already had a placeholder curveball module in shared JSON and a reserved deck slide, which meant a one-off memo would create drift instead of confidence.
- Decision: upgrade the placeholder into a real response, add a detailed playbook under `docs/plans/`, and push the same structure into the landing-page rail.
- Guardrail: keep the top-line budget unchanged; show the curveball as internal reallocation and fast operational flex, not as a new funding ask.

### 2:36 PM CT

- Working question: how should the package handle the conflicting weekday/date pair in the produce brief?
- Current belief: keep produce timing abstract as a `48-hour` perishables window and describe the sequence as `Day 0`, `Day 1`, and `Day 2`.
- Evidence or rationale: the supplied text says "Sunday evening (March 30)," but March 30, 2026 is a Monday. Abstract timing prevents the deck and demo from baking in a factual conflict.
- Decision: use abstract perishables timing in the deck, docs, and demo.
- Guardrail: keep the cold-chain steps concrete even while the calendar labels stay abstract.

### 2:35 PM CT

- Working question: what does a strong developer handoff need now that the tracker proof exists?
- Current belief: the existing competition docs are too broad for implementation handoff. The developer needs one concise tracker PRD, a strict visual/spec file, and executable user stories.
- Evidence or rationale: the tracker is already scoped and working, so the leverage point is specificity: exact quality bar, interaction rules, states, and data assumptions rather than more concept framing.
- Decision: add a one-page tracker PRD with JTBD, a root `BRAND.md`, and a tracker-specific user-stories doc for engineering handoff.
- Guardrail: keep these docs implementation-facing. Do not re-open the larger StreetCart concept or broaden the scope beyond the stop-first tracker.

### 2:45 PM CT

- Working question: how should the developer handoff actually be passed over?
- Current belief: one merged master doc is better than three separate implementation docs for first-pass handoff, as long as product, brand, and stories stay clearly separated inside it.
- Evidence or rationale: the developer does not need discovery archaeology. He needs one reading path, one quality bar, and one build order.
- Decision: add a root `MASTER_DOC.md` that merges the tracker PRD, JTBD, brand system, component rules, technical constraints, user stories, and recommended build sequence into one implementation document.
- Guardrail: preserve the separate source docs, but treat `MASTER_DOC.md` as the handoff default.

### 3:05 PM CT

- Working question: where should a user-provided benchmark table live so it is reusable instead of becoming another chat-only artifact?
- Current belief: preserve it as a structured research dataset under `docs/research/` and keep source IDs exactly as provided until the team decides to map them to full citations.
- Evidence or rationale: the repo already stores narrative benchmark memos, but not a machine-readable comparison table that deck or product work can reuse directly. Keeping `Not in source` explicit avoids accidental invention.
- Decision: add `docs/research/2026-03-28-food-access-program-benchmark-dataset/programs.json` plus a short README documenting assumptions and citation gaps.
- Guardrail: do not treat the source ID list as resolved bibliography data yet.
